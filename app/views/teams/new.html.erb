
<%= form_for [:fan, @team] do |f| %>
<%= f.label :name %>
<%= f.text_field :team_name %>
<%= f.collection_select(:queen_ids, Queen.all, :id, :name, {}, {:multiple => true})%>
<%= f.collection_select(:queen_ids, Queen.all, :id, :name, {}, {:multiple => true})%>
<%= f.collection_select(:queen_ids, Queen.all, :id, :name, {}, {:multiple => true})%>
<%= f.collection_select(:queen_ids, Queen.all, :id, :name, {}, {:multiple => true})%>
<%= f.collection_select(:queen_ids, Queen.all, :id, :name, {}, {:multiple => true})%>
<%= f.collection_select(:queen_ids, Queen.all, :id, :name, {}, {:multiple => true})%>
<%= f.submit %>
<%end%>
